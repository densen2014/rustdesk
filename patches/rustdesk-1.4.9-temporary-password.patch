diff --git a/src/core_main.rs b/src/core_main.rs
index 6b437a9..8a10fc3 100644
--- a/src/core_main.rs
+++ b/src/core_main.rs
@@ -471,6 +471,19 @@ pub fn core_main() -> Option<Vec<String>> {
         } else if args[0] == "--get-id" {
             println!("{}", crate::ipc::get_id());
             return None;
+        } else if args[0] == "--get-temporary-password" {
+            match crate::ipc::get_temporary_password() {
+                Ok(password) if !password.is_empty() => println!("{}", password),
+                Ok(_) => {
+                    eprintln!("RustDesk returned an empty temporary password");
+                    std::process::exit(1);
+                }
+                Err(err) => {
+                    eprintln!("Failed to read temporary password: {err}");
+                    std::process::exit(1);
+                }
+            }
+            return None;
         } else if args[0] == "--set-id" {
             if is_cli_setting_change_disabled() {
                 println!("Settings are disabled!");
diff --git a/src/ipc.rs b/src/ipc.rs
index 68c987f..e04a631 100644
--- a/src/ipc.rs
+++ b/src/ipc.rs
@@ -1588,6 +1588,10 @@ pub fn get_fingerprint() -> String {
 pub fn get_fingerprint() -> String {
     get_config("fingerprint")
         .unwrap_or_default()
         .unwrap_or_default()
 }
+
+pub fn get_temporary_password() -> ResultType<String> {
+    Ok(get_config("temporary-password")?.unwrap_or_default())
+}
 
 pub fn set_permanent_password(v: String) -> ResultType<()> {
     if Config::is_disable_change_permanent_password() {
        bail!("Changing permanent password is disabled");